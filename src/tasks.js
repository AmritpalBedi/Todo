const todoList = []

const projects = []

const task = () => {
    return {
        title: null,
        description: null,
        dueDate: null,
        priority: null,
        notes: null,
        checklist: null,
    }
}

